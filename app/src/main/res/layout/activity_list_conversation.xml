<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:atlas="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">
            <!--The stuff commented out was an alternate way to enable.disable availibility. Ignore it 4 now -Ryan-->
            <!--<LinearLayout
                android:id="@+id/enable_or_disable_availability"
                android:orientation="horizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="20dp">
                <TextView
                    android:id="@+id/enable_or_disable_availability_text"
                    android:layout_width="298dp"
                    android:layout_height="56dp"
                    android:gravity="center"
                    android:text="Check to flag yourself as available"/>
                <CheckBox
                    android:id="@+id/enable_or_disable_availability_checkbox"
                    android:layout_width="48dp"
                    android:layout_height="40dp"
                    android:layout_margin="5dp"
                    android:layout_marginEnd="20dp"
                    android:layout_marginRight="20dp"/>
            </LinearLayout>-->
            <HorizontalScrollView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@color/counselor_bar"
                android:scrollbars="none">
                <LinearLayout
                    android:id="@+id/counselorbar"
                    android:layout_width="match_parent"
                    android:layout_height="90dp"
                    android:orientation="horizontal"
                    android:gravity="center"
                    android:background="@color/counselor_bar">

                </LinearLayout>
            </HorizontalScrollView>

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent">
                <com.layer.atlas.AtlasConversationsList
                    android:id="@+id/conversationlist"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"/>

                <!--<View
                    android:id="@+id/newconversation"
                    android:layout_width ="72dp"
                    android:layout_height="72dp"
                    android:layout_gravity="bottom|right"
                    android:layout_marginRight="8dp"
                    android:layout_marginBottom="4dp"
                    android:gravity="center"
                    android:background="@drawable/atlas_ctl_btn_plus_big" />-->
            </FrameLayout>
        </LinearLayout>
    </FrameLayout>

    <!-- The navigation drawer (LEFT) -->
    <ListView android:id="@+id/left_drawer"
        android:layout_width="240dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#ffffffff">
    </ListView>

    <!-- The navigation drawer (RIGHT) -->
    <ListView android:id="@+id/right_drawer"
        android:layout_width="240dp"
        android:layout_height="match_parent"
        android:layout_gravity="right"
        android:background="#ffffffff">
    </ListView>


</android.support.v4.widget.DrawerLayout>